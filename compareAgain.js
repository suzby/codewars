function solution(str, ending){
    let endingLength = ending.length - 1
    let toCompare = str.slice(endingLength)
    return toCompare === ending ? true : false
  
  }

  //missing something?? samurai